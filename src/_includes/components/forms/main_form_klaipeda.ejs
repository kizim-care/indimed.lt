<%
    let selfDescription = 'Į Jūsų užklausą sureaguos tinkamiausias „IndiMed“ draugijos sveikatingumo specialistas, kuris telefonu suteiks trumpą konsultaciją ir gydymo rekomendacijas. Jei reikės, paskirs Jums vizitą ir suteiks gydymo paslaugas Jūsų mieste.'
    let selfImage = 'https://kizim-lt.b-cdn.net/indimed.lt/ta7IaBrDbEjtQ0vw94QfHAfB64C67SwDF5BiL44B.webp'

    if (typeof description !== 'undefined' && description.length > 0) {
        selfDescription = description;
    }

    if (typeof image !== 'undefined' && image.length > 0) {
        selfImage = image;
    }
%>

<%- include('../main_form.ejs', {
    description: selfDescription,
    image: selfImage,
    fields: [
        _formFields.patientAge,
        _formFields.description,
        {
            type: 'hidden',
            data: {
                name: _formFields.city.data.name,
                value: _const.cityKlaipeda,
            },
        },
        _formFields.canArrive,
        _formFields.preferedLocation, // <-- Conditional field
        _formFields.name,
        _formFields.email,
        _formFields.phone,
    ],
}) %>
